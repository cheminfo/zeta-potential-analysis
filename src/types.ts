import type { ZetaPotentialMeta } from 'cheminfo-types';

/** Wrapper for standardized cheminfo metadata in zeta potential spectra. */
export interface ZetaPotentialCheminfo {
  /** Standardized zeta potential metadata. */
  meta: ZetaPotentialMeta;
}
